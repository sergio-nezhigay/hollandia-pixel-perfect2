{{ 'breadcrumbs.css' | asset_url | stylesheet_tag }}

{%- if product -%}
  {%- render 'breadcrumbs', collection: product.collections.first, class: 'px-4 pt-4 xl:px-8 xl:pt-6' -%}
{%- elsif collection -%}
  {%- render 'breadcrumbs', collection: collection, class: 'px-4 pt-4 xl:px-8 xl:pt-6' -%}
{%- endif -%}

{%- schema -%}
{
  "name": "Product breadcrumbs",
  "class": "product-breadcrumbs-section"
}
{%- endschema -%}
